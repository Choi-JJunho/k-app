# Optional override file.
# If present, this file is uploaded to /opt/stacks/projects/k-app/.env before deploy.
# DB values are still auto-configured from /opt/stacks/supabase/.env on homeamd.

# Recommended: set your own app JWT secret (otherwise Supabase JWT_SECRET is reused).
KAPP_JWT_SECRET=<your-app-jwt-secret>

# Optional
# KAPP_JWT_ACCESS_TOKEN_EXPIRE_DAYS=7
